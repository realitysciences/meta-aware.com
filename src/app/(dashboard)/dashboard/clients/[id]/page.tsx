import FirebaseComingSoon from '@/components/FirebaseComingSoon'

export default function ClientDetailPage() {
  return (
    <FirebaseComingSoon
      title="Source details are being rebuilt"
      description="This old detail view depended on Supabase client/session tables. The Firebase version should become a source-detail page tied to the Whole-Person Atlas."
    />
  )
}
