import FirebaseComingSoon from '@/components/FirebaseComingSoon'

export default function AnalysesPage() {
  return (
    <FirebaseComingSoon
      title="Lens Scans are moving to Firestore"
      description="Analysis results now save through Firebase. This legacy analyses page needs a new Firestore query model before it becomes the Lens Scans archive."
    />
  )
}
